/*
let table_contents = document.getElementById('table-of-contents');
let form = document.getElementById('custom-form'); 



window.addEventListener('scroll',()=>{
  let cut_point = form.offsetTop;
  if(pageYOffset >= 100 && pageYOffset < cut_point - 800){
    table_contents.classList.add('sticky');
  }else{
    table_contents.classList.remove('sticky');
  }
  console.log(cut_point);
});
*/
